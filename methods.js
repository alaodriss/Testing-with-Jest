/*
beforeAll(fn, timeout)
beforeEach(fn, timeout)
*/

beforeAll(
// connect to database
//empty testing table
// add dummy data for testin
// prepare everything before testing
)


afterAll(
    // clean database
      // clean caches
)